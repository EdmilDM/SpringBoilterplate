package com.edmil.boilerplate.repository;

import com.edmil.boilerplate.model.DanceOff;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DanceOffRepository extends JpaRepository<DanceOff, Long> {

}
